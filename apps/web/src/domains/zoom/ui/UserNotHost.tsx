import { Text } from "@chakra-ui/react";
import React from "react";

export const UserNotHost = () => (
  <Text>
    You need to be the host of the meeting to record the interview and create
    highlights. You are not the host of this meeting. Please create a new
    meeting and ask the participant to join
  </Text>
);
